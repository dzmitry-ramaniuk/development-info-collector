# Visitor (Посетитель)

## Содержание

1. [Идея паттерна](#идея-паттерна)
2. [Участники](#участники)
3. [Пример на Java](#пример-на-java)
4. [Плюсы и ограничения](#плюсы-и-ограничения)
5. [Вопросы для самопроверки](#вопросы-для-самопроверки)

## Идея паттерна

**Visitor** отделяет операции от структуры объектов и позволяет добавлять новые операции без изменения классов элементов.

## Участники

- **Visitor** — интерфейс операций;
- **ConcreteVisitor** — конкретная операция;
- **Element** — интерфейс с методом `accept`;
- **ObjectStructure** — набор элементов.

## Пример на Java

```java
interface Shape {
    void accept(ShapeVisitor visitor);
}

interface ShapeVisitor {
    void visit(Circle circle);
    void visit(Rectangle rectangle);
}
```

## Плюсы и ограничения

- плюс: легко добавлять новые операции;
- минус: тяжело добавлять новые типы элементов;
- полезен для AST, отчётов, валидации сложных моделей.

## Вопросы для самопроверки

1. Почему Visitor удобен для компиляторов?
2. Какой главный недостаток при эволюции модели?
3. Когда лучше заменить Visitor на полиморфизм?
