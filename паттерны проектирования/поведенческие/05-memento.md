# Memento (Хранитель)

## Содержание

1. [Идея паттерна](#идея-паттерна)
2. [Участники](#участники)
3. [Пример на Java](#пример-на-java)
4. [Практические советы](#практические-советы)
5. [Вопросы для самопроверки](#вопросы-для-самопроверки)

## Идея паттерна

**Memento** позволяет сохранять и восстанавливать состояние объекта, не раскрывая внутреннюю структуру.

## Участники

- **Originator** — объект, состояние которого сохраняется.
- **Memento** — снимок состояния.
- **Caretaker** — хранит историю снимков.

## Пример на Java

```java
record EditorMemento(String text) {}

class TextEditor {
    private String text = "";

    void write(String value) { text += value; }
    EditorMemento save() { return new EditorMemento(text); }
    void restore(EditorMemento memento) { text = memento.text(); }
    String text() { return text; }
}
```

## Практические советы

- ограничивайте глубину истории (например, ring buffer);
- для больших состояний храните дельты, а не полный снимок;
- следите за безопасностью, если состояние содержит чувствительные данные.

## Вопросы для самопроверки

1. Почему Memento лучше ручного копирования полей?
2. Какие компромиссы по памяти возникают?
3. Как сочетать Memento и Command для undo/redo?
